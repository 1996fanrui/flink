2026 May 13

Quick recap
Roman and Rui discussed two different approaches for handling state recovery and checkpointing in their system. Rui presented his current proposal involving a recovered buffer store and input channel modifications, while Roman suggested an alternative approach of pre-filtering and rewriting task state snapshots. They explored the challenges of managing disk references, buffer pools, and synchronization during checkpoint operations. The discussion covered technical details about locks, file handling, and the trade-offs between complexity and code reusability. They agreed to compare both approaches further before making a final decision, with Rui planning to analyze the options in more detail over the next few days.
Next steps
Rui
- Compare the two different approaches (current PR approach and Roman's proposal) for handling disk buffer references and checkpointing, and evaluate which is simpler and more correct.
- Look into how to coordinate/synchronize between task state snapshot and input channel for Roman's proposed approach, especially regarding data readiness and correct offsets during checkpointing.
- If the final conclusion is to proceed with the current PR approach, start simplifying the PR by removing unnecessary locks (dispatcher lock and Gatelock during add buffers) and potentially flattening buffer store logic as discussed.
- Conduct a proof of concept (POC) to determine if the Gatelock can be safely removed for the relevant code paths.
- Consider whether to keep or remove the recovered input channel, and if kept, document its usage for cases where checkpointing during recovery is disabled.
- Evaluate the impact of random IO during checkpointing if files are shared across channels, and proceed with simplifying the code by removing complex sequential IO logic if agreed.
- Share findings and next steps with Roman after comparing approaches and simplifying the PR as appropriate.
Summary
Disk Buffer Recovery Approaches Discussion
Rui and Roman discussed approaches for handling disk buffers and input channels during recovery. Rui proposed maintaining a recovered buffer store that would decouple components like the dispatcher and input channel. Roman suggested a simpler approach where they would download, filter, and write data to disk before starting recovery, potentially reconstructing state handles without modifying existing code. They clarified that when checkpointing during recovery is disabled, the system would still need to consume data through the recovered input channel before requesting partitions. The discussion ended with Roman questioning whether their proposed approach of downloading and filtering everything preemptively would still be necessary given Rui's buffer store proposal.
State Filtering and Checkpointing Approaches
Rui and Roman discussed two approaches for handling state filtering and checkpointing. Roman proposed filtering data in advance and reorganizing it into new state handlers without using a buffer store, while Rui expressed concerns about potential data loss and blocking issues if buffer pools are insufficient. Rui explained that even with Roman's approach, they would still need buffer stores to handle cases where network buffers are insufficient, as spilling to disk would be required to avoid blocking during checkpoint operations.
File Synchronization and Snapshots Discussion
Roman and Rui discussed approaches for handling file snapshots and synchronization between input channels and task state. They identified a need for synchronization due to potential conflicts when reading and writing files simultaneously. Rui explained that maintaining disk references at the input channel level is necessary due to locking constraints, as different offsets need to be managed for snapshots. Both agreed that the current approach using channel-level locks might not align well with task-level snapshots, highlighting a potential challenge in implementing the proposed solution.
PR Simplification Progress Discussion
Roman and Rui discussed Rui's progress on updating a PR, with Rui explaining he was still working on simplifying the approach. Rui proposed removing the complex disk buffer logic and simplifying the dispatcher to handle random IO instead of maintaining sequential IO during checkpoints. They agreed that some form of memory cache would still be needed, particularly if they maintain disk references at the input channel level, and Rui confirmed he would remove the git lock as part of the simplification.
Buffer Storage and Checkpointing Approaches
Roman and Rui discussed two different approaches for handling buffer storage and checkpointing in their system. They aligned on using a single file per channel instead of sharing files across channels, and agreed to keep the recovered buffer store as a separate class to simplify the code. Rui will compare the two approaches and work on simplifying the code, while Roman will be offline on Friday. They identified a potential concern about ensuring data readiness and correct offset snapshots during checkpointing, which Rui will investigate further.