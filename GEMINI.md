## 代码生成规则

请严格遵守以下规则：
- 使用 TDD 开发模型，请设计完善的单元测试, 修改或新增代码前请先完成 test 代码的变更
- 代码如何改动了一些协议，请同时修改依赖项，防止下游的逻辑失败
- 不要使用虚构的 API 或不存在的库。
- 遇到不确定内容请标注 TODO 或注释说明。
- 不要编造类名、方法名或内容。
- 输出完整函数或类，不要省略。
- 完成代码生成任务后，请检查已生成的代码是否包含任何不存在的方法、类或 API。

## 运行与测试

每次修改完请务必运行相关测试 ，如果有报错请自动修复，直到报错被解决后才停止。

注：如果测试失败是因为测试覆盖率不足，请提升覆盖率而不是 降低测试覆盖率的要求。

## 编写测试

在添加测试时，请首先检查现有的测试文件，以理解并遵循已建立的约定。

## Git 仓库

本项目的主要分支名为 "main"。

## 注释策略

只在必要时编写高价值的注释。避免通过注释与用户对话。注释的重点应该是解释"为什么"这样做，而不是"做了什么"，特别是对于复杂的逻辑。
